Get rid/improve/redo GammaEx and LambdaEx, they could just be a contained containing a map and some interface functions

Separate Node class in two class with inheritance.

Replace SetOfNodes, BeepVector and StrStrMap for std or boost structures

Improve website.

Add genome to nodes (branchlenght)

List of scenarios to chose from in the User Interfance

Cmake cross plataform with automatic installation of packages.

Make Cmake compatible to WIndows,Mac and other UNIX platforms

Time of nodes a variable in the node.

Separate ownertrees methods in node.

add more feactures to nodes that can be changed by clicking on it, also the position.

Conecction to trees database to use those trees.

posibility to drag and drop nodes

improve the way the LGT cordinates are being calculated by including a time event to the LGT edge

Get rid/redo the Newick/PRIME tree parser, the current one uses C code and bison and flex.
